apiVersion: app.k8s.io/v1beta1
kind: Application
metadata:
  name: guestbook2-app
  namespace: guestbook2
spec:
  componentKinds:
  - group: apps.open-cluster-management.io
    kind: Subscription
  descriptor: {}
  selector:
    matchExpressions:
    - key: app
      operator: In
      values:
      - guestbook2-app