    kind: Service
    apiVersion: v1
    metadata:
      name: redis-secondary
    spec:
      type: ClusterIP
      ports:
        - port: 6379
      selector:
        app: redis
        role: secondary
        tier: backend
